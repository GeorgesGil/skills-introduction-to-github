// Dependency preparation is intentionally performed only inside the isolated
// validation container. This host-side compatibility file executes no package
// manager and must never process repository-controlled npm configuration.
